# Orchestrator framework: crewai | autogen (lazy-loaded at runtime)
ORCHESTRATOR_FRAMEWORK=crewai

# Audio I/O: mock (CLI keyboard/print) | http (Windows host bridge)
AUDIO_DRIVER=mock

# Local Ollama model (one at a time; hardware-friendly defaults)
# Default piano: qwen2.5:3b — se WSL ha poca RAM, preferire gemma2:2b
# (vedi docs/ollama.md e output di scripts/bench_ollama_intent.py).
OLLAMA_MODEL=qwen2.5:3b
# OLLAMA_BASE_URL=http://127.0.0.1:11434
# Se Ollama gira sull'host Windows, es.:
# OLLAMA_BASE_URL=http://$(awk '/^nameserver/{print $2;exit}' /etc/resolv.conf):11434

# Windows host for audio HTTP bridge (WSL2). Leave empty to discover
# from /etc/resolv.conf nameserver; set to override.
WINDOWS_HOST=
WINDOWS_AUDIO_PORT=8765

# Cloud LLM (sandbox lab: python -m sandbox.ollama_fs_lab --llm openai)
# Chiave obbligatoria con --llm openai; modello opzionale (default gpt-4o-mini).
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# ANTHROPIC_API_KEY=
