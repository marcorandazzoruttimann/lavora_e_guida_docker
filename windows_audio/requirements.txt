# Venv *host Windows* (python.exe Win32), non il progetto WSL.
# Elenco e tabella: README.md «Ambienti: WSL2 vs Host Windows».
# Non aggiungere queste dipendenze a pyproject.toml: l'orchestratore
# non deve importare pygame né parlare con edge-tts in prima persona.
#
# Fuori da questo file (non sono pip): SDK .NET 8 per stt_helper,
# pacchetto lingua it-IT + FOD Speech, microfono; se le DLL native
# di vosk/pygame non caricano, VC++ Redistributable x64.
# Dettaglio operativo: docs/audio_host.md
#
# Avvio tipico (PowerShell su Windows, cartella windows_audio/):
#   py -3.12 -m venv .venv
#   .\.venv\Scripts\pip install -r requirements.txt
#   .\.venv\Scripts\python server.py

# Sintesi neurale Microsoft (rete). Voce di prodotto: it-IT-ElsaNeural.
edge-tts>=6.1.1
# Playback locale MP3 fino alla fine: tiene il mutex half-duplex.
# Il wheel Windows porta SDL_mixer; non serve un'installazione SDL a parte.
pygame>=2.5.0
# Dettatura italiana dopo la wake WinRT (topic cloud UserCanceled sul helper Win32).
vosk>=0.3.45
sounddevice>=0.4.6
numpy>=1.26.0
# Durata MP3 per il playback; se manca si stima dal bitrate.
mutagen>=1.47.0
